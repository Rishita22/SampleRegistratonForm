version : "3.8"
services:
  webservice:
    build:
      context: "/home/ec2-user"
      dockerfile: "Dockerfile.httpd"
    ports:
      - "90:80"

  appservice:
    build:
      context: "/home/ec2-user"
      dockerfile: "Dockerfile"
    ports:
      - "100:8080"
